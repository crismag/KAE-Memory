{
  "//": "Claude Code \u2014 register with: claude mcp add-json kae-memory \"$(cat config/mcp/claude-code.json)\"",
  "//command": "Absolute path: the executable lives in the project venv and is not on PATH when a client spawns it. A bare name fails with 'Failed to connect' and no further detail.",
  "//provider": "PostgreSQL + pgvector is the default local provider. For CockroachDB set KAE_DATABASE_PROVIDER=cockroachdb and KAE_COCKROACHDB_URL instead.",
  "command": "/mnt/ai/workspaces/KAE-Memory/.venv/bin/kae-memory-mcp",
  "args": [
    "serve"
  ],
  "env": {
    "KAE_DATABASE_PROVIDER": "postgresql",
    "KAE_POSTGRESQL_URL": "postgresql+psycopg://kae:CHANGE_ME@localhost:5432/kae_memory",
    "KAE_ENVIRONMENT": "local"
  }
}
