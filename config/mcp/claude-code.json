{
  "//": "Claude Code — register with: claude mcp add-json kae-memory \"$(cat config/mcp/claude-code.json)\"",
  "//command": "Absolute path: the executable lives in the project venv and is not on PATH unless that venv is active. A bare name fails with 'Failed to connect' and no further detail.",
  "command": "/mnt/ai/workspaces/KAE-Memory/.venv/bin/kae-memory-mcp",
  "args": ["serve"],
  "env": {
    "KAE_DATABASE_PROVIDER": "cockroachdb",
    "KAE_DATABASE_URL": "cockroachdb+psycopg://root@localhost:26259/kae_dev?sslmode=disable",
    "KAE_ENVIRONMENT": "local"
  }
}
