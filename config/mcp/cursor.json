{
  "//": "Cursor \u2014 place at .cursor/mcp.json in the target repository, or merge into ~/.cursor/mcp.json",
  "//command": "Absolute path: the executable lives in the project venv and is not on PATH when a client spawns it.",
  "//provider": "PostgreSQL + pgvector is the default local provider.",
  "mcpServers": {
    "kae-memory": {
      "command": "/mnt/ai/workspaces/KAE-Memory/.venv/bin/kae-memory-mcp",
      "args": [
        "serve"
      ],
      "env": {
        "KAE_DATABASE_PROVIDER": "postgresql",
        "KAE_POSTGRESQL_URL": "postgresql+psycopg://kae:CHANGE_ME@localhost:5432/kae_memory",
        "KAE_ENVIRONMENT": "local"
      }
    }
  }
}
